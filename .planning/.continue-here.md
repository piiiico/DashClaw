# Continue here — AgentLens absorption (2026-05-13)

## Status

**Done and on `origin/main`** — 13 commits (`345344bd..06855be9`). The AgentLens absorption shipped end-to-end. Live ingest against `~/.claude/projects` succeeded for 285 sessions.

Work was `/goal`-driven from `AGENTLENS_INTEGRATION_GOAL.md`, not phase-tracked under `.planning/phases/`, so there is no phase directory for this work. The next phase-driven work resumes from wherever `STATE.md` left off — this handoff is only for the AgentLens absorption follow-ups.

## Commit chain (all on `main`)

```
06855be9 fix(cli): retry-with-backoff + light throttle on code ingest
b9ee04fe fix(code-sessions): move route-level tagged-template SQL into repository
0e59ca79 fix(code-sessions): wrap UI pages in PageLayout so the sidebar renders
a1e31df0 fix(code-sessions): correct ../ depth in projects/[projectId]/sessions route
993d9466 feat(code-sessions): Phases 7-9 — autopsy, ROI, memos, cleanup scripts, MCP resources
e0574089 feat(code-sessions): Optimal Files routes + MCP tools (Phase 6)
366d219e feat(code-sessions): signals/alerts wiring + weekly cron + learning bridge + UI
8286ea37 feat(cli): dashclaw code subcommand group — Path B JSONL backfill
eea55c5b feat(hooks): opt-in Stop-hook code-session reporter (Phase 3)
78c98257 feat(code-sessions): schema, repository, ingest endpoint, cache-aware pricing
cb84f240 feat(claude-code): port AgentLens algorithmic core as pure ESM modules
345344bd docs(agentlens): land integration goal + bump SDK header to v2.12.0
```

## Test counts at pause

- vitest: 2028 passing, 5 skipped (149 new under `__tests__/unit/claude-code/`)
- Python hooks: 276 passing, 2 skipped (5 new for the Stop reporter + fail-silent regression)
- CLI: 12 passing via `cd cli && npm test`
- `npm run lint`: clean
- `npm run build`: clean
- `npm run route-sql:check`: 85 current vs 90 baseline (under, passing)

## Live verification (already done by Wes)

- `dashclaw code ingest` against real `~/.claude/projects`: **285 ingested, 261 errors (all HTTP 429)**.
- 429s are now mitigated by the retry-with-backoff added in `06855be9`. Next ingest run should pick up the 261 stragglers.

## What still needs to happen (operator-side)

1. **Re-run `dashclaw code ingest`** to pick up the 261 sessions that 429'd before the retry fix. Expect them to ingest cleanly now.
2. **`npm run db:migrate`** against the production Postgres (already done locally per the live run success — verify it actually applied `drizzle/0006_code_sessions.sql` on prod).
3. **Manual smoke list from the goal**:
   - Hook ingest path A: run a short real Claude Code session with `DASHCLAW_CODE_SESSIONS_ENABLED=1`, confirm a `source='hook'` row lands.
   - Navigate Projects → Session → Detail; check the Mission Control reconciliation tile shows both numbers side-by-side per A10.
   - Confirm the "governed" badge cross-links to `/replay/<action_id>` for tool_uses that mapped to action_records.
   - Mission Control Agent Spend (30D) tile unchanged for `agent_id='claude-code'` (bit-identical to pre-Phase-3).
   - Generate Optimal Files on a real session → manifest → `dashclaw code apply <manifestId> --dest=...`.
4. **MCP client smoke** — verify `dashclaw_optimal_files_preview` and `dashclaw_optimal_files_manifest` are discoverable end-to-end via your MCP client.
5. **DashClaw memory update** — add a note to `~/clawd/memory/projects/dashclaw.md` that DashClaw now includes Code Sessions telemetry. Deferred from Phase 8 because that file lives outside this repo.

## Known follow-ups / open questions

- **Rate-limit threshold for ingest** — Vercel's per-IP limit kicked in at ~285 POSTs from one session. If you'll regularly backfill larger trees, consider:
  - Adding `/api/code-sessions/ingest-jsonl` to a quota-exempt or higher-limit class in middleware.
  - OR increasing the CLI throttle floor from 150 ms to 300 ms.
- **Mission Control "Operations Feed" check** — the goal's A11 / verification gate says `code_session_alerts` must NOT appear in the existing Operations Feed. I didn't touch the operations-feed query, so this should already hold — but verify after the first session that triggers an alert.
- **AgentLens repo memory note** — Phase 8 deliverable. `RevenueGoalExperiment-V3/ARCHIVED.md` was written (sibling repo) but the goal also asks to update its `README.md` top section to "Archived; absorbed into DashClaw — see ARCHIVED.md." Not done.
- **Dependabot moderate vuln on `main`** — preexisting, surfaced again on every push. Unrelated to this work.

## Resume command

```
/goal Continue AGENTLENS_INTEGRATION_GOAL.md follow-up: re-run dashclaw code ingest to pick up the 261 previously-429'd sessions, then walk the manual smoke list at .planning/.continue-here.md sections 1-4. Don't re-do any of the 13 committed phases; just the operator-side verification + the small README archive update.
```
