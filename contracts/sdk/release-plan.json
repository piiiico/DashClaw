{
  "node": {
    "current_version": "4.1.2",
    "next_bump": "none",
    "reason": "4.1.2 — patch. Platform security/reliability hardening (authoritative risk persistence, durable guard audit, shared agent-identity resolution on actions+x402, x402 input/provider/endpoint validation + reference redaction + outcome sync, outcome lifecycle gating). No SDK API changes; the Node SDK republishes at 4.1.2 per the unified-version model. Self-dep stays ^4.0.0.",
    "domains": []
  },
  "python": {
    "current_version": "4.1.2",
    "next_bump": "none",
    "reason": "4.1.2 — patch. Platform security/reliability hardening (see Node entry). No Python SDK API changes; the package republishes at 4.1.2 per the unified-version model. Additive; no breaking changes.",
    "domains": []
  }
}
