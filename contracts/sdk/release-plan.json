{
  "node": {
    "current_version": "4.2.3",
    "next_bump": "none",
    "reason": "4.2.3 — patch. Docs-accuracy sweep: PROJECT_DETAILS.md now references the migrated source files by their real .ts paths (23 stale app/lib/*.js + app/api/**/route.js references corrected to .ts after the TypeScript migration). No code, public-surface, or SDK change — Node surface stays 126; the SDK is byte-identical to 4.2.2, so republishing at 4.2.3 is optional. Self-dep stays ^4.0.0.",
    "domains": ["docs"]
  },
  "python": {
    "current_version": "4.2.3",
    "next_bump": "none",
    "reason": "4.2.3 — patch. Docs-accuracy sweep only (PROJECT_DETAILS.md .js→.ts source paths). No Python SDK change — surface stays 224; byte-identical to 4.2.2, republish optional.",
    "domains": ["docs"]
  }
}
