{
  "node": {
    "current_version": "4.0.0",
    "next_bump": "none",
    "reason": "4.0.0 staged (sdk/package.json bumped; publish via `npm run release:sdks`). BREAKING: removed the archived context-thread methods createThread/addThreadEntry/closeThread (/api/context/threads*) — removed in source by bbbb517b; they only ever 404'd (no next.config rewrite; context_threads table never existed). Canonical public-method count is 104. The root package.json self-dependency stays at ^3.0.0 until 4.0.0 is published to npm — bumping it earlier desyncs package-lock.json (registry-pinned 3.0.0) and breaks `npm ci`.",
    "domains": []
  },
  "python": {
    "current_version": "4.0.0",
    "next_bump": "none",
    "reason": "4.0.0 staged (sdk-python/pyproject.toml bumped; publish via `npm run release:sdks`). BREAKING: removed the archived context-thread methods create_thread/add_thread_entry/close_thread/get_threads (211 -> 207, also bbbb517b) and the key-point methods capture_key_point/get_key_points/get_context_summary (/api/context/points; 207 -> 204) — all targeted the archived /api/context/* and only ever 404'd. Public-method count is 204.",
    "domains": []
  }
}
