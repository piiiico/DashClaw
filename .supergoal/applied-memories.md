# Applied memories (inherited into this migration plan)

Pulled from the DashClaw auto-memory index. Each line: what it is → how it shapes the plan.

- **finops_spend_subsystem / x402_spend_governance_decision** → Fleet Spend = Agent LLM Spend + x402 Purchase Spend; x402_purchase EXCLUDED from Agent LLM Spend; Claude Code spend advisory/separate. Confirmed in code (`finops.repository.js`, `actions.repository.js:1037/1049/1063 AND action_type <> 'x402_purchase'`). These are hard invariants — drive Phase 5/6 and the FinOps/x402 adversarial reviewers.
- **cost_estimate is a write-time snapshot** → stored cost must NOT be repriced during aggregation; pricing fixes need a backfill script, never an aggregation-time change. Drives the "no repricing" invariant.
- **billing.js authoritative / pricing.js analytics** → two pricing tables, intentional differences (unknown→$0 vs Sonnet-fallback; substring-includes vs exact-key). `rate-card-parity.test.js` guards Claude-model parity. Shared TS source must preserve BOTH fallback semantics. `scripts/refresh-model-pricing.mjs` rewrites GENERATED markers in BOTH files — converting to .ts must keep that working.
- **pg numeric returns strings** → Neon HTTP gives numeric cols as strings; coerce with `Number()` before summing. Typed row mappers must model these as string→number coercion, not silent `number`.
- **Route SQL guardrail (no direct SQL in routes)** → routes go through repositories; `route-sql:check` blocks increases. TS conversion of routes must not introduce direct SQL; repositories exempt.
- **ON CONFLICT-vs-index + actions table is `action_records`** → DB-semantics class of prod-500s the mocked suite can't catch; verify DB fixes against real Postgres. Reinforces "do not alter DB semantics" approval gate.
- **Testable pages must be .jsx (Vite .js loader won't parse JSX)** → already true; TSX conversion is compatible. `app/secrets/` needs `!app/secrets/` negation (gitignore trap) — watch when touching that area.
- **SDK unified version (platform + node + python share ONE version), enforced by version:sync:check** → migration must not desync `package.json`/`sdk/package.json`/`sdk-python/pyproject.toml`. SDK public surface must stay stable (no method add/remove without approval). Python SDK is OUT of scope (not converted).
- **No PRs — commit/push to main** (feedback) → BUT this run is explicitly NO-COMMIT without approval. Reconcile: do the work uncommitted; deliverable audit uses repo-state.sh against the working tree (committed+staged+unstaged+untracked), never baseline..HEAD.
- **A push is its own turn / full test suite in plan verification** (feedback) → verification gate runs the FULL `npx vitest run`, not targeted patterns, at major integration phases and final audit. Never claim done on a targeted run.
- **Verify live state before claiming root cause / read file before explaining** (feedback) → adversarial reviewers must cite real file+line evidence; reject claims without evidence. DB-semantics findings need real-Postgres verification, not the mocked suite.
- **CodeQL false positives settled** (feedback) → don't "fix" generateApiKey hash, slugify/email ReDoS, safeFetch SSRF during the migration; they're settled.
- **livingcode/openapi/api-inventory/route-sql baseline are GENERATED** → never hand-edit; regenerate via the documented scripts; pre-commit hook regenerates. TS extension changes must not break these generators (tooling-hotspots lane checks this).
- **Never hardcode version numbers** (CLAUDE.md) → `next.config.js` injects `NEXT_PUBLIC_DASHCLAW_VERSION` from package.json; `version:check` fails the build on hardcodes. Survive through TS.
- **Never hardcode hex; read .impeccable.md before UI work** → TSX/UI phase preserves CSS tokens + `/spend/code` token-resolved Recharts brand-orange.
